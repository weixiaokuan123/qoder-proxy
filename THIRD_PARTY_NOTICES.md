# 第三方开源声明（Third-Party Notices）

> 本文档记录本项目在设计、实现上参考或借鉴的开源项目，以及各自的许可证与合规要求。
>
> 若你对本文件的完整性有疑问或发现遗漏，请提交 issue 或 PR。

## 一、项目缘起

本项目是**本机 Qoder 桌面端登录态**的只读访问层、自动签到工具，以及基于官方 CLI 的
推理入口。它不参考任何"Qoder 接入方案"类第三方项目——管理面的上游协议（端点、请求头、
活动/领取接口）均通过对 Qoder 官方客户端的**独立观察**（应用日志、内嵌前端资源、
实际请求验证）得出。

## 二、代码复用

本项目的两处实现直接复用自同目录下的姊妹项目（同一作者）：

| 复用的模块 | 来源 | 用途 | 许可 |
|---|---|---|---|
| `src/scheduler.ts` | `../workbuddy-proxy/src/scheduler.ts` | 每日随机时刻签到调度器（原作者注释已声明"三个代理共用同一套逻辑"） | **MIT** |

`src/auth.ts`（Electron `safeStorage` / Chromium `v10` 解密）与 `src/upstream.ts`
（Qoder 上游协议）为本项目独立实现；解密思路属于 Electron / Chromium 的公开行为，
非某项目的私有实现。

## 三、官方依赖

推理面**打包并调用官方 CLI**，不是自研协议实现：

| 依赖 | 来源 | 用途 | 许可 |
|---|---|---|---|
| `@qoder-ai/qodercli` | npm（Qoder 官方发布） | 本地推理运行时，经其 `-p` 无头模式调用 | 见包内 `LICENSE`（"SEE LICENSE IN LICENSE"） |

> 该包体积较大（约 68MB），装在 `vendor/` 且 `vendor/node_modules` **不入版本库**，
> 由使用者自行 `npm install` 获取。本项目只调用其公开命令行接口，不修改、不再分发其代码。

`src/cli.ts` 为本项目对上述 CLI 的封装，是独立实现。

## 四、参考调研

以下项目在**调研阶段**被查阅，用于确认"Qoder 推理无法通过公开 REST 调用"这一结论，
其代码**未被复制**进本项目：

| 项目 | 仓库 | 参考内容 | 许可 |
|---|---|---|---|
| `sontianye/qoder-api` | <https://github.com/sontianye/qoder-api> | 确认「包装 qodercli 子进程」是社区既有路线 | MIT |

> 本项目独立得出相同结论（CLI 子进程转发），实现细节与之无关。

## 五、上游协议说明

Qoder 的 API 端点、请求头与活动接口属于 Qoder 服务的实现细节，其服务条款可能限制
非官方客户端的自动化访问。**本项目仅供个人在本机管理自己账号的签到与额度查询使用**，
使用者需自行承担合规责任。
