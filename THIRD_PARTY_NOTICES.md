# 第三方开源声明（Third-Party Notices）

> 本文档记录本项目在设计、实现上参考或借鉴的开源项目，以及各自的许可证与合规要求。
>
> 若你对本文件的完整性有疑问或发现遗漏，请提交 issue 或 PR。

## 一、项目缘起

本项目是**本机 Qoder 桌面端登录态**的只读访问层与自动签到工具。它不参考任何
"Qoder 接入方案"类第三方项目——上游协议（端点、请求头、活动/领取接口）均通过对
Qoder 官方客户端的**独立观察**（应用日志、内嵌前端资源、实际请求验证）得出。

## 二、代码复用

本项目的两处实现直接复用自同目录下的姊妹项目（同一作者）：

| 复用的模块 | 来源 | 用途 | 许可 |
|---|---|---|---|
| `src/scheduler.ts` | `../workbuddy-proxy/src/scheduler.ts` | 每日随机时刻签到调度器（原作者注释已声明"三个代理共用同一套逻辑"） | **MIT** |

`src/auth.ts`（Electron `safeStorage` / Chromium `v10` 解密）与 `src/upstream.ts`
（Qoder 上游协议）为本项目独立实现；解密思路属于 Electron / Chromium 的公开行为，
非某项目的私有实现。

## 三、参考实现

以下项目在**验证思路**时被查阅，其代码**未被复制**进本项目：

| 项目 | 仓库 | 参考内容 | 许可 |
|---|---|---|---|
| `qoder-proxy` | <https://github.com/avaritiachaos/qoder-proxy> | 一份基于 `qodercli` CLI 包装的 Qoder 代理，用于确认上游认证方式的公开已知信息 | 见其仓库 |

> 该项目采用 CLI 子进程转发（`qodercli login` / `QODERCN_PERSONAL_ACCESS_TOKEN`），
> 与本项目"只读桌面端凭据 + 直连 REST"的路线不同，仅作背景调研。

## 四、上游协议说明

Qoder 的 API 端点、请求头与活动接口属于 Qoder 服务的实现细节，其服务条款可能限制
非官方客户端的自动化访问。**本项目仅供个人在本机管理自己账号的签到与额度查询使用**，
使用者需自行承担合规责任。
